<div class="content">
        {{#resetPasswordForm}}
        <form class="private-form" method="post" action="{{url}}">
            <fieldset>
                <div class="p-control-head">Пароль</div>
                {{#errors.password}}<div class="p-control-info">{{errors.password}}</div>{{/errors.password}}
                <div class="p-control-group incorrect"><!-- класс incorrect - для ошибки-->
                    <label class="incorrect-error">Старый пароль <span class="err">некорректный</span></label><!--Можно лейбл аппендить сюда, если ошибка?-->
                    <input id="old-password" name="old-password" type="password" placeholder="Старый пароль" class="p-control p-control-input">
                </div>
                <div class="p-control-group">
                        <input id="new-password" name="new-password" type="password" placeholder="Новый пароль" class="p-control p-control-input">
                </div>
                <div class="p-control-group">
                        <input id="confirm-password" name="confirm-password" type="password" placeholder="Подтверждение пароля" class="p-control p-control-input">
                </div>
                <div class="p-control-group btn-container">
                        <input type="submit" value="Сохранить пароль" class="btn btn--submit btn--orange btn--medium"/>
                </div>
            </fieldset>
        </form>
        {{/resetPasswordForm}}
</div>