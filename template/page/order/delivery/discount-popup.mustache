{{! ввести код скидки или сертефиката }}
<div class="flexbox-two-rows">
    <div class="checkout-sale-top checkout-order__box flexbox-two-rows__header">
        <div class="checkout-sale-code popup-fixed-content">
            <div class="checkout-sale-code__title">Код скидки или подарочный сертификат</div>

            <form class="js-discount-form checkout-sale-code__form" action="{{url}}" method="post">
                <input type="hidden" name="{{fields.order.name}}" value="{{fields.order.value}}" />

                <div class="checkout-sale-code__fields">
                    <input type="text" class="checkout-sale-code__it textfield" name="{{fields.number.name}}" data-field="number" />

                    <div class="checkout-sale-code-pin">
                        <label class="checkout-sale-code-pin__label">PIN-код</label>
                        <input type="text" class="checkout-sale-code__it checkout-sale-code__it--pin textfield" name="{{fields.pin.name}}"  data-field="pin" />
                    </div>
                </div>

                <button class="checkout-sale-code__button btn-type btn-type--default js-order-btn" data-btn="selected-sale">Применить</button>
            </form>
        </div>
    </div>

    <div class="flexbox-two-rows__content flexbox-two-rows js-user-discount-container popup-fixed-content" data-url="/ajax/user/coupons"></div>
</div>
{{! ввести код скидки или сертефиката }}