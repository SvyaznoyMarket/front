<div class="map-baloon clearfix">
 <div class="shop-snippet">
    <div class="shop-snippet-name">
        <img src="/img/points/logos/100x100/{{icon}}" alt="" class="shop-snippet-name__img">
        <span class="shop-snippet-name__text">{{group.name}}</span>
    </div>

    <div class="shop-snippet-info">
        {{#subway}}
        <div class="shop-snippet-metro">
            <span class="shop-snippet-metro__bullet" style="background: {{color}};"></span>
            <span class="shop-snippet-metro__text">{{name}}</span>
        </div>
        {{/subway}}

        <div class="shop-snippet-address">{{address}} <br/>{{regime}}</div>
    </div>

</div>


    <div class="deliv-info-snippet">
        <span class="deliv-info-snippet__date">{{date.name}}</span>
        <span class="deliv-info-snippet__price">
            {{#cost.name}}{{cost.name}} <span class="rubl">p</span>{{/cost.name}}
            {{^cost.name}}Бесплатно{{/cost.name}}
        </span>
    </div>

    <button class="btn-choose-point js-order-delivery-form-control" data-value="{{&dataValue}}" data-type="point">Выбрать</button>
</div>